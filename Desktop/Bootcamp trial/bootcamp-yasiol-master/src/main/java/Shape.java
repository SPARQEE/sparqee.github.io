public interface Shape {
	public double parameter();
	public double area();
	//Usually don't write any code in interface; what you can do with shape: find area
	//and parameter, no code on how to do it, just way to define method,
	//not how it is executed
}